# PostgreSQL Configuration (used by docker-compose.yml)
POSTGRES_USER=curio
POSTGRES_PASSWORD=curio_password
POSTGRES_DB=curio_db

# Note: API key is backend-only now. Frontend uses relative /api paths via nginx reverse proxy.
